/**
 * 扩展常量定义
 * 用于避免循环依赖问题
 */

// 扩展完整ID
const EXTENSION_PUBLISHER = "xingwangzhe";
const EXTENSION_NAME = "rustedwarfaremodsupport";
export const EXTENSION_ID = `${EXTENSION_PUBLISHER}.${EXTENSION_NAME}`;
